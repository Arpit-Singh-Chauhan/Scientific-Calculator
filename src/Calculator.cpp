#include "Calculator.h"
#include <iostream>

using namespace std;

void Calculator::get_two_numbers(string first, string second){
    cout << first;
    cin >> a;

    cout << second;
    cin >> b;
}

void Calculator::get_one_number(string val){
    cout << val;
    cin >> a;
}